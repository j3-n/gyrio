package components

// ToolbarEntry stores the information about a single entry on the toolbar
type ToolbarEntry struct {
	// The key prompt to show for this action
	Key string
	// The text to accompany the key prompt
	Text string
}
